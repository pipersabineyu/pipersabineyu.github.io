export type Experiment = {
  title: string;
  src: string;
  poster?: string;
};

// iPhone-frame UI prototypes shown in the home page's "Experiments"
// section — quick standalone explorations that don't warrant a full case
// study, unlike the projects covered in the hero scroll-through. Empty
// until the actual prototype clips/screenshots are added.
export const experiments: Experiment[] = [];
